{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Go Mastery Path","text":"<p>A structured, module-wise Go training program that takes you from your first <code>go run</code> to production-grade, master-level engineering \u2014 with runnable code snippets in every module and a hands-on project at the end of each level.</p>"},{"location":"#how-the-program-is-organized","title":"How the program is organized","text":"Level Focus Modules Level 1 \u00b7 Entry Language foundations 9 topics + 1 project Level 2 \u00b7 Intermediate Idiomatic, testable Go 9 topics + 1 project Level 3 \u00b7 Advanced Concurrency, APIs, databases 9 topics + 1 project Level 4 \u00b7 Master Production systems &amp; architecture 9 topics + 1 capstone"},{"location":"#how-to-use-this-site","title":"How to use this site","text":"<ul> <li>Work through each level in order \u2014 later modules assume earlier ones.</li> <li>Every topic page has runnable code snippets \u2014 copy them into a local <code>.go</code>   file and run with <code>go run</code>, or <code>go build</code> a binary.</li> <li>Each level ends with a project that combines everything learned in that level.</li> <li>Use the search bar (top of the page) to jump straight to a topic.</li> </ul> <p>Start here \u2192 Level 1 \u00b7 Entry</p> <p>\ud83c\udfa5 Prefer video? Watch the Mastery Path video series on YouTube \u2014 Shorts and full walkthroughs of these lessons.</p>"},{"location":"#more-from-the-mastery-path-series","title":"More from the Mastery Path series","text":"<p>Free, structured, module-wise training across 31 other languages and platforms:</p> <p>Languages</p> \ud83d\udc0d Python \u2615 Java \ud83d\udfe8 JavaScript \ud83d\udd37 TypeScript \ud83d\udc1a Shell/Bash \ud83d\udcbb PowerShell \ud83c\udde8 C \u2795 C++ \ud83e\udd80 Rust \ud83d\uddc4\ufe0f SQL \ud83d\udc8e Ruby \ud83d\udc18 PHP \ud83d\udfe3 Kotlin \ud83d\udc26 Swift \ud83c\udfaf Dart \ud83d\udd34 Scala \ud83d\udcca R <p>Cloud Platforms</p> \u2601\ufe0f AWS \u2601\ufe0f Azure \u2601\ufe0f GCP \u2601\ufe0f IBM Cloud \ud83c\udfa8 Adobe <p>AI / ML / LLM</p> \ud83e\udd16 AI/ML \ud83e\udde0 LLM Dev \ud83d\udcda RAG \ud83d\udcf1 Edge AI <p>Embedded Systems</p> \ud83d\udd0c Embedded \ud83d\udc27 Embedded Linux \ud83d\udc0d Embedded Python \u23f1\ufe0f FreeRTOS \ud83d\udd27 S32K"},{"location":"privacy/","title":"Privacy Policy","text":"<p>Last updated: 2026-07-18</p> <p>This site (\"Go Mastery Path\") uses Google AdSense to display advertising.</p>"},{"location":"privacy/#cookies-data-collection","title":"Cookies &amp; data collection","text":"<p>Google AdSense and its partners may use cookies, device identifiers, or similar technologies to serve ads based on your prior visits to this or other websites. Google's use of advertising cookies enables it and its partners to serve ads based on your visit to this site and/or other sites on the Internet.</p> <p>You can opt out of personalized advertising by visiting Google's Ads Settings or www.aboutads.info/choices.</p>"},{"location":"privacy/#what-this-site-does-not-do","title":"What this site does not do","text":"<p>This site does not require you to create an account, does not collect personal information through forms, and does not sell any data it might receive to third parties beyond the standard operation of the AdSense advertising network described above.</p>"},{"location":"privacy/#contact","title":"Contact","text":"<p>Questions about this policy can be directed via the GitHub repository for this site.</p>"},{"location":"level-1/","title":"Level 1 \u00b7 Entry Foundations","text":"<p>Goal: get comfortable reading and writing Go, understand its core building blocks, and ship a small working program.</p>"},{"location":"level-1/#modules","title":"Modules","text":"<ol> <li>Setup &amp; First Program</li> <li>Variables, Types &amp; Operators</li> <li>Control Flow</li> <li>Functions &amp; Multiple Returns</li> <li>Arrays, Slices &amp; Maps</li> <li>Structs</li> <li>Pointers</li> <li>Error Handling</li> <li>Packages &amp; Modules</li> <li>Project \u2014 CLI To-Do App</li> </ol> <p>By the end of this level you'll be able to write programs that take input, make decisions, store and process data with slices and maps, handle failure idiomatically with Go's error values, and organize code into packages backed by a Go module.</p>"},{"location":"level-1/01-setup/","title":"01 \u00b7 Setup &amp; First Program","text":""},{"location":"level-1/01-setup/#install-go","title":"Install Go","text":"<p>Go ships as a single toolchain: compiler, formatter, test runner, and package manager all live behind one <code>go</code> command. Install a recent stable release:</p> <pre><code># macOS (Homebrew)\nbrew install go\n\n# Ubuntu/Debian\nsudo apt install golang-go\n\n# Windows / any OS: download the installer from https://go.dev/dl/\n</code></pre> <p>Verify the install:</p> <pre><code>go version\n# go version go1.22.x darwin/arm64\n</code></pre> <p><code>GOPATH</code> and workspace conventions used to matter a lot; modern Go (1.16+) works fine anywhere on disk once you're inside a module \u2014 no special folder required.</p>"},{"location":"level-1/01-setup/#your-first-program","title":"Your first program","text":"<p>Create a directory and a file called <code>main.go</code>:</p> <pre><code>// main.go\npackage main\n\nimport \"fmt\"\n\nfunc main() {\n    fmt.Println(\"Hello, world!\")\n}\n</code></pre> <p>Every runnable Go program has a <code>package main</code> and a <code>func main()</code> \u2014 that's the entry point the Go runtime looks for. <code>import \"fmt\"</code> pulls in the standard library's formatted-I/O package so you can use <code>fmt.Println</code>.</p>"},{"location":"level-1/01-setup/#running-and-building","title":"Running and building","text":"<pre><code>go run main.go\n# Hello, world!\n</code></pre> <p><code>go run</code> compiles to a temporary binary, executes it, and cleans up \u2014 perfect for quick iteration. For a real, distributable binary:</p> <pre><code>go build main.go\n# produces an executable named \"main\" (or \"main.exe\" on Windows)\n\n./main\n# Hello, world!\n</code></pre> <p><code>go build</code> compiles to a static binary with no external runtime dependency \u2014 you can copy that file to another machine of the same OS/architecture and it will just work.</p>"},{"location":"level-1/01-setup/#anatomy-of-the-program","title":"Anatomy of the program","text":"Piece Meaning <code>package main</code> Declares this file belongs to the <code>main</code> package \u2014 the one that produces an executable. <code>import \"fmt\"</code> Brings in a standard-library package for formatted I/O. <code>func main()</code> The program's entry point \u2014 no parameters, no return value. <code>fmt.Println(...)</code> Prints its arguments followed by a newline to standard output. No semicolons Go statements end at the newline; the compiler inserts semicolons for you."},{"location":"level-1/01-setup/#gofmt-one-style-no-debates","title":"gofmt: one style, no debates","text":"<p>Go ships an opinionated formatter. Run it on save (most editors do this automatically) and every Go codebase looks the same:</p> <pre><code>gofmt -l .        # list files that aren't formatted\ngofmt -w main.go  # rewrite a file in place\ngo fmt ./...      # gofmt across an entire module\n</code></pre>"},{"location":"level-1/01-setup/#choosing-an-editor","title":"Choosing an editor","text":"<p>VS Code with the official \"Go\" extension (free, includes <code>gopls</code> \u2014 the Go language server \u2014 for autocomplete, jump-to-definition, and inline errors) is the most common choice. GoLand (JetBrains) is a strong paid alternative with deeper refactoring tools. Either works well; pick one and move on.</p>"},{"location":"level-1/01-setup/#exercise","title":"Exercise","text":"<p>Write a program <code>greeter.go</code> with a <code>main</code> function that prints a greeting for three different names, one per line, using <code>fmt.Println</code>. Run it with <code>go run greeter.go</code>, then compile it to a binary with <code>go build</code> and run the resulting executable directly.</p>"},{"location":"level-1/02-variables-types-operators/","title":"02 \u00b7 Variables, Types &amp; Operators","text":""},{"location":"level-1/02-variables-types-operators/#declaring-variables","title":"Declaring variables","text":"<p>Go is statically typed but infers types for you most of the time. Two ways to declare a variable:</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    // var form: explicit type, works anywhere (including package scope)\n    var age int = 30\n    var name string = \"Ada\"\n\n    // type inference: var without a type\n    var city = \"Boston\"\n\n    // short declaration :=  -- only inside function bodies\n    country := \"USA\"\n\n    fmt.Println(age, name, city, country)\n}\n</code></pre> <p><code>:=</code> is the idiomatic choice inside functions. <code>var</code> is required at package level, when you want a zero value with no initializer, or when the inferred type would be wrong (e.g. you want <code>float64</code> but wrote a whole number).</p>"},{"location":"level-1/02-variables-types-operators/#zero-values","title":"Zero values","text":"<p>Unlike many languages, Go never leaves a declared variable \"undefined\" \u2014 it gets a zero value automatically:</p> <pre><code>var count int      // 0\nvar price float64   // 0.0\nvar label string     // \"\" (empty string)\nvar ready bool       // false\nvar data []int       // nil (nil slice, safe to read, len is 0)\n</code></pre>"},{"location":"level-1/02-variables-types-operators/#basic-types","title":"Basic types","text":"Category Types Integers <code>int</code>, <code>int8/16/32/64</code>, <code>uint</code>, <code>uint8</code> (<code>byte</code>), <code>uint16/32/64</code> Floating point <code>float32</code>, <code>float64</code> Text <code>string</code>, <code>rune</code> (an alias for <code>int32</code>, holds a Unicode code point) Boolean <code>bool</code> Complex (rare) <code>complex64</code>, <code>complex128</code> <p><code>int</code> is the general-purpose choice \u2014 its size (32 or 64 bit) matches the platform's native word size. Use a specific width (<code>int64</code>, <code>uint8</code>) only when the size matters, e.g. binary protocols or memory-tight structures.</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    var temperature float64 = 98.6\n    var initial byte = 'A'          // byte is an alias for uint8\n    var letter rune = '\u65e5'           // rune holds one Unicode code point\n\n    fmt.Println(temperature, initial, string(letter))\n    fmt.Printf(\"%T %T %T\\n\", temperature, initial, letter)\n    // float64 uint8 int32\n}\n</code></pre>"},{"location":"level-1/02-variables-types-operators/#constants-and-iota","title":"Constants and <code>iota</code>","text":"<pre><code>package main\n\nimport \"fmt\"\n\nconst Pi = 3.14159 // untyped constant, adapts to context\n\nconst (\n    StatusPending = iota // 0\n    StatusActive         // 1\n    StatusDone            // 2\n)\n\nfunc main() {\n    fmt.Println(Pi, StatusPending, StatusActive, StatusDone)\n}\n</code></pre> <p><code>iota</code> resets to <code>0</code> at each <code>const (...)</code> block and increments by one per line \u2014 the standard idiom for defining a small enum-like set of related constants.</p>"},{"location":"level-1/02-variables-types-operators/#type-conversion","title":"Type conversion","text":"<p>Go never converts types implicitly. Mixing an <code>int</code> and a <code>float64</code> in an expression is a compile error \u2014 you must convert explicitly:</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    var i int = 10\n    var f float64 = 3.0\n\n    // result := i + f          // compile error: mismatched types\n    result := float64(i) + f\n    fmt.Println(result) // 13\n\n    back := int(result) // truncates, does not round: 13\n    fmt.Println(back)\n}\n</code></pre>"},{"location":"level-1/02-variables-types-operators/#operators","title":"Operators","text":"<pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    a, b := 17, 5\n\n    fmt.Println(a + b)  // 22\n    fmt.Println(a - b)  // 12\n    fmt.Println(a * b)  // 85\n    fmt.Println(a / b)  // 3   (integer division truncates)\n    fmt.Println(a % b)  // 2   (remainder)\n\n    // Comparison\n    fmt.Println(a &gt; b, a == b, a != b) // true false true\n\n    // Logical\n    fmt.Println(a &gt; 10 &amp;&amp; b &lt; 10) // true\n    fmt.Println(a &lt; 10 || b &lt; 10) // true\n\n    // Increment/decrement are statements, not expressions\n    a++\n    b--\n    fmt.Println(a, b) // 18 4\n}\n</code></pre>"},{"location":"level-1/02-variables-types-operators/#cheat-sheet","title":"Cheat sheet","text":"Concept Syntax Declare with inference <code>x := 5</code> Declare with explicit type <code>var x int = 5</code> Zero value <code>var x int</code> \u2192 <code>0</code> Constant <code>const Pi = 3.14</code> Enum-like constants <code>const ( A = iota; B; C )</code> Convert types <code>float64(x)</code>, <code>int(y)</code> Multiple assignment <code>a, b := 1, 2</code>"},{"location":"level-1/02-variables-types-operators/#exercise","title":"Exercise","text":"<p>Write a program that declares a rectangle's <code>width</code> and <code>height</code> as <code>float64</code> using <code>:=</code>, computes its area and perimeter, and prints both with <code>fmt.Printf</code> using the <code>%.2f</code> verb for two decimal places. Then add an <code>int</code> constant block using <code>iota</code> for three shape types (<code>Rectangle</code>, <code>Circle</code>, <code>Triangle</code>) and print the constant matching your rectangle.</p>"},{"location":"level-1/03-control-flow/","title":"03 \u00b7 Control Flow","text":""},{"location":"level-1/03-control-flow/#if-else-no-parentheses-required","title":"if / else \u2014 no parentheses required","text":"<pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    score := 82\n\n    if score &gt;= 90 {\n        fmt.Println(\"A\")\n    } else if score &gt;= 80 {\n        fmt.Println(\"B\")\n    } else {\n        fmt.Println(\"C or below\")\n    }\n}\n</code></pre> <p>Braces <code>{}</code> are mandatory in Go, even for single-statement bodies \u2014 there is no one-line <code>if</code> without braces.</p>"},{"location":"level-1/03-control-flow/#if-with-an-initializer-statement","title":"if with an initializer statement","text":"<p>A very common Go idiom: run a statement, then check its result, scoped only to the <code>if</code>/<code>else</code>:</p> <pre><code>package main\n\nimport (\n    \"fmt\"\n    \"strconv\"\n)\n\nfunc main() {\n    if value, err := strconv.Atoi(\"42\"); err == nil {\n        fmt.Println(\"parsed:\", value)\n    } else {\n        fmt.Println(\"parse failed:\", err)\n    }\n    // value and err do not exist out here\n}\n</code></pre> <p>This pattern shows up constantly with functions that return <code>(result, error)</code> \u2014 see Module 8.</p>"},{"location":"level-1/03-control-flow/#for-gos-only-loop-keyword","title":"for \u2014 Go's only loop keyword","text":"<p>Go has exactly one looping construct, <code>for</code>, which covers every case other languages split across <code>for</code>/<code>while</code>/<code>do-while</code>:</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    // Classic three-part for\n    for i := 0; i &lt; 5; i++ {\n        fmt.Println(\"count:\", i)\n    }\n\n    // \"while\" loop -- condition only\n    n := 5\n    for n &gt; 0 {\n        fmt.Println(\"n is\", n)\n        n--\n    }\n\n    // Infinite loop -- break to exit\n    total := 0\n    for {\n        total++\n        if total == 3 {\n            break\n        }\n    }\n    fmt.Println(\"total:\", total)\n\n    // range over a slice: index, value\n    fruits := []string{\"apple\", \"banana\", \"cherry\"}\n    for i, fruit := range fruits {\n        fmt.Println(i, fruit)\n    }\n\n    // range when you only want values\n    for _, fruit := range fruits {\n        fmt.Println(\"fruit:\", fruit)\n    }\n}\n</code></pre>"},{"location":"level-1/03-control-flow/#continue-and-labeled-loops","title":"continue and labeled loops","text":"<pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    for i := 1; i &lt;= 10; i++ {\n        if i%2 != 0 {\n            continue // skip odd numbers\n        }\n        fmt.Println(i)\n    }\n\nouter:\n    for i := 0; i &lt; 3; i++ {\n        for j := 0; j &lt; 3; j++ {\n            if j == 1 {\n                continue outer // continue the OUTER loop\n            }\n            fmt.Println(i, j)\n        }\n    }\n}\n</code></pre>"},{"location":"level-1/03-control-flow/#switch-no-automatic-fallthrough","title":"switch \u2014 no automatic fallthrough","text":"<p>Unlike C or Java, a Go <code>switch</code> case does not fall through by default \u2014 each case breaks automatically. Use the <code>fallthrough</code> keyword to opt in.</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    day := \"Tue\"\n\n    switch day {\n    case \"Sat\", \"Sun\":\n        fmt.Println(\"Weekend\")\n    case \"Mon\", \"Tue\", \"Wed\", \"Thu\", \"Fri\":\n        fmt.Println(\"Weekday\")\n    default:\n        fmt.Println(\"Unknown\")\n    }\n\n    // switch with no expression -- reads like an if/else chain\n    score := 82\n    switch {\n    case score &gt;= 90:\n        fmt.Println(\"A\")\n    case score &gt;= 80:\n        fmt.Println(\"B\")\n    default:\n        fmt.Println(\"C or below\")\n    }\n\n    // switch with an initializer, same idea as if\n    switch x := 2; x {\n    case 1:\n        fmt.Println(\"one\")\n    case 2:\n        fmt.Println(\"two\")\n        fallthrough // explicitly fall into the next case\n    case 3:\n        fmt.Println(\"three (reached via fallthrough or directly)\")\n    }\n}\n</code></pre>"},{"location":"level-1/03-control-flow/#cheat-sheet","title":"Cheat sheet","text":"Construct Example If <code>if x &gt; 0 { ... }</code> If/else if/else <code>if ... else if ... else { ... }</code> If with initializer <code>if v, err := f(); err == nil { ... }</code> Classic for <code>for i := 0; i &lt; n; i++ { ... }</code> While-style <code>for cond { ... }</code> Infinite <code>for { ... break }</code> Range (index, value) <code>for i, v := range slice { ... }</code> Range (value only) <code>for _, v := range slice { ... }</code> Switch <code>switch x { case 1: ...; default: ... }</code> Switch, no expression <code>switch { case cond: ... }</code>"},{"location":"level-1/03-control-flow/#exercise","title":"Exercise","text":"<p>Write a program that loops from 1 to 30 and, using <code>switch</code> with no expression, prints <code>\"FizzBuzz\"</code> for multiples of 15, <code>\"Fizz\"</code> for multiples of 3, <code>\"Buzz\"</code> for multiples of 5, and the number itself otherwise. Then add a second loop using <code>for</code> with <code>range</code> over a slice of your favorite foods that skips (<code>continue</code>) any name shorter than 4 characters.</p>"},{"location":"level-1/04-functions-multiple-returns/","title":"04 \u00b7 Functions &amp; Multiple Returns","text":""},{"location":"level-1/04-functions-multiple-returns/#basic-function-syntax","title":"Basic function syntax","text":"<pre><code>package main\n\nimport \"fmt\"\n\n// name(params) returnType\nfunc add(a int, b int) int {\n    return a + b\n}\n\n// consecutive parameters of the same type can share one annotation\nfunc multiply(a, b int) int {\n    return a * b\n}\n\nfunc main() {\n    fmt.Println(add(2, 3))      // 5\n    fmt.Println(multiply(4, 5)) // 20\n}\n</code></pre>"},{"location":"level-1/04-functions-multiple-returns/#multiple-return-values","title":"Multiple return values","text":"<p>This is one of Go's signature features \u2014 functions routinely return more than one value, most commonly a result and an error:</p> <pre><code>package main\n\nimport (\n    \"errors\"\n    \"fmt\"\n)\n\nfunc divide(a, b float64) (float64, error) {\n    if b == 0 {\n        return 0, errors.New(\"division by zero\")\n    }\n    return a / b, nil\n}\n\nfunc main() {\n    result, err := divide(10, 2)\n    if err != nil {\n        fmt.Println(\"error:\", err)\n    } else {\n        fmt.Println(\"result:\", result)\n    }\n\n    _, err = divide(10, 0)\n    if err != nil {\n        fmt.Println(\"error:\", err) // error: division by zero\n    }\n}\n</code></pre> <p>The blank identifier <code>_</code> discards a return value you don't need \u2014 Go forces you to either use every declared variable or explicitly discard it.</p>"},{"location":"level-1/04-functions-multiple-returns/#named-return-values","title":"Named return values","text":"<p>Return values can be named up front; a bare <code>return</code> sends back their current values. Useful for short functions and for documenting intent, but overusing it can hurt readability in longer functions.</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc divmod(a, b int) (quotient, remainder int) {\n    quotient = a / b\n    remainder = a % b\n    return // \"naked\" return -- sends back quotient, remainder\n}\n\nfunc main() {\n    q, r := divmod(17, 5)\n    fmt.Println(q, r) // 3 2\n}\n</code></pre>"},{"location":"level-1/04-functions-multiple-returns/#variadic-functions","title":"Variadic functions","text":"<p>A parameter prefixed with <code>...</code> accepts zero or more arguments, collected into a slice inside the function:</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc sum(nums ...int) int {\n    total := 0\n    for _, n := range nums {\n        total += n\n    }\n    return total\n}\n\nfunc main() {\n    fmt.Println(sum())           // 0\n    fmt.Println(sum(1, 2, 3))    // 6\n\n    values := []int{10, 20, 30}\n    fmt.Println(sum(values...)) // spread a slice with ...\n}\n</code></pre>"},{"location":"level-1/04-functions-multiple-returns/#functions-as-values-closures","title":"Functions as values, closures","text":"<p>Functions are first-class values in Go \u2014 assign them to variables, pass them as arguments, return them from other functions.</p> <pre><code>package main\n\nimport \"fmt\"\n\n// takes a function as a parameter\nfunc applyTwice(f func(int) int, x int) int {\n    return f(f(x))\n}\n\n// returns a closure that captures \"start\"\nfunc counterFrom(start int) func() int {\n    count := start\n    return func() int {\n        count++\n        return count\n    }\n}\n\nfunc main() {\n    double := func(x int) int { return x * 2 }\n    fmt.Println(applyTwice(double, 3)) // 12\n\n    next := counterFrom(10)\n    fmt.Println(next()) // 11\n    fmt.Println(next()) // 12\n    fmt.Println(next()) // 13\n}\n</code></pre>"},{"location":"level-1/04-functions-multiple-returns/#defer-run-code-on-function-exit","title":"defer \u2014 run code on function exit","text":"<p><code>defer</code> schedules a call to run right before the surrounding function returns, regardless of how it returns. It's the idiomatic way to guarantee cleanup (closing files, unlocking mutexes) sits right next to the code that acquired the resource.</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc process() {\n    fmt.Println(\"start\")\n    defer fmt.Println(\"cleanup (runs last)\")\n    fmt.Println(\"middle\")\n    // \"cleanup\" prints after \"middle\", right before process() returns\n}\n\nfunc main() {\n    process()\n    // start\n    // middle\n    // cleanup (runs last)\n}\n</code></pre> <p>Multiple <code>defer</code> calls run in LIFO order (last deferred, first executed).</p>"},{"location":"level-1/04-functions-multiple-returns/#cheat-sheet","title":"Cheat sheet","text":"Feature Syntax Basic function <code>func add(a, b int) int { return a + b }</code> Multiple returns <code>func f() (int, error) { ... }</code> Named returns <code>func f() (x int) { x = 1; return }</code> Variadic <code>func f(nums ...int) { ... }</code> Spread a slice <code>f(values...)</code> Discard a return <code>_, err := f()</code> Function value <code>var f func(int) int = double</code> Closure <code>func() func() int { ... }</code> Deferred cleanup <code>defer file.Close()</code>"},{"location":"level-1/04-functions-multiple-returns/#exercise","title":"Exercise","text":"<p>Write a function <code>minMax(nums ...int) (min, max int)</code> using named return values that finds the smallest and largest numbers in a variadic list of ints (return <code>0, 0</code> for an empty call). In <code>main</code>, call it with a slice spread using <code>...</code>, and add a <code>defer</code> statement that prints <code>\"done\"</code> after the results are printed.</p>"},{"location":"level-1/05-arrays-slices-maps/","title":"05 \u00b7 Arrays, Slices &amp; Maps","text":""},{"location":"level-1/05-arrays-slices-maps/#arrays-fixed-size-rarely-used-directly","title":"Arrays: fixed size, rarely used directly","text":"<p>An array's length is part of its type \u2014 <code>[3]int</code> and <code>[5]int</code> are different types entirely. Because of this rigidity, arrays are uncommon in everyday Go code; slices (below) are what you'll actually reach for.</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    var nums [3]int          // [0 0 0] -- zero-valued\n    nums[0] = 10\n    nums[1] = 20\n    nums[2] = 30\n\n    fruits := [3]string{\"apple\", \"banana\", \"cherry\"}\n    sized := [...]string{\"a\", \"b\"} // let the compiler count: [2]string\n\n    fmt.Println(nums, fruits, sized, len(fruits))\n}\n</code></pre>"},{"location":"level-1/05-arrays-slices-maps/#slices-the-workhorse-collection","title":"Slices: the workhorse collection","text":"<p>A slice is a flexible, growable view over an underlying array \u2014 three words under the hood: a pointer, a length, and a capacity.</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    // Slice literal\n    fruits := []string{\"apple\", \"banana\", \"cherry\"}\n    fmt.Println(fruits, len(fruits)) // [apple banana cherry] 3\n\n    // make(type, length, capacity)\n    scores := make([]int, 0, 10) // len 0, capacity 10 -- avoids reallocation\n\n    // append grows the slice, returns a (possibly new) slice\n    scores = append(scores, 90, 85, 77)\n    fmt.Println(scores, len(scores), cap(scores))\n\n    // slicing: [low:high), high is exclusive\n    fmt.Println(fruits[0:2]) // [apple banana]\n    fmt.Println(fruits[1:])  // [banana cherry]\n    fmt.Println(fruits[:2])  // [apple banana]\n}\n</code></pre>"},{"location":"level-1/05-arrays-slices-maps/#slices-share-underlying-arrays","title":"Slices share underlying arrays","text":"<p>Slicing does not copy data \u2014 two slices can point at the same backing array, so mutating one can affect the other:</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    original := []int{1, 2, 3, 4, 5}\n    view := original[1:4] // [2 3 4], shares memory with original\n\n    view[0] = 99\n    fmt.Println(original) // [1 99 3 4 5] -- original changed too!\n\n    // To get an independent copy, use copy()\n    independent := make([]int, len(view))\n    copy(independent, view)\n    independent[0] = -1\n    fmt.Println(view) // unaffected: [99 3 4]\n}\n</code></pre>"},{"location":"level-1/05-arrays-slices-maps/#two-dimensional-slices","title":"Two-dimensional slices","text":"<pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    grid := make([][]int, 3) // 3 rows\n    for i := range grid {\n        grid[i] = make([]int, 3) // each row: 3 columns\n    }\n    grid[1][1] = 5\n\n    for _, row := range grid {\n        fmt.Println(row)\n    }\n    // [0 0 0]\n    // [0 5 0]\n    // [0 0 0]\n}\n</code></pre>"},{"location":"level-1/05-arrays-slices-maps/#maps-key-value-pairs","title":"Maps: key-value pairs","text":"<pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    // Map literal\n    ages := map[string]int{\n        \"Alice\": 30,\n        \"Bob\":   25,\n    }\n\n    // make() for an empty map\n    scores := make(map[string]int)\n    scores[\"Carol\"] = 88\n    scores[\"Dave\"] = 92\n\n    fmt.Println(ages[\"Alice\"]) // 30\n\n    // the \"comma ok\" idiom -- check whether a key exists\n    value, ok := ages[\"Eve\"]\n    fmt.Println(value, ok) // 0 false -- zero value if missing, ok tells you why\n\n    // delete a key\n    delete(scores, \"Dave\")\n\n    // iterate -- order is NOT guaranteed\n    for name, score := range scores {\n        fmt.Println(name, score)\n    }\n\n    fmt.Println(len(scores))\n}\n</code></pre> <p>The comma-ok idiom (<code>value, ok := m[key]</code>) is essential: reading a missing key returns the zero value silently, so <code>ok</code> is the only reliable way to distinguish \"key present with zero value\" from \"key absent.\"</p>"},{"location":"level-1/05-arrays-slices-maps/#cheat-sheet","title":"Cheat sheet","text":"Operation Syntax Array (fixed size) <code>var a [3]int</code> Slice literal <code>s := []int{1, 2, 3}</code> Slice with make <code>s := make([]int, len, cap)</code> Append <code>s = append(s, 4, 5)</code> Slice a slice <code>s[1:3]</code> Copy independently <code>copy(dst, src)</code> Map literal <code>m := map[string]int{\"a\": 1}</code> Map with make <code>m := make(map[string]int)</code> Read + existence check <code>v, ok := m[key]</code> Delete a key <code>delete(m, key)</code> Length (slice or map) <code>len(s)</code>"},{"location":"level-1/05-arrays-slices-maps/#exercise","title":"Exercise","text":"<p>Write a program that builds a <code>map[string]int</code> counting word frequency in a <code>[]string</code> of words (some repeated). Iterate the map to print each word and its count, then use <code>append</code> to build a <code>[]string</code> slice containing only the words that appear more than once, using the comma-ok idiom where useful.</p>"},{"location":"level-1/06-structs/","title":"06 \u00b7 Structs","text":""},{"location":"level-1/06-structs/#defining-and-creating-a-struct","title":"Defining and creating a struct","text":"<p>A <code>struct</code> groups related fields into a single named type \u2014 Go's equivalent of a lightweight class body without inheritance.</p> <pre><code>package main\n\nimport \"fmt\"\n\ntype Person struct {\n    Name string\n    Age  int\n    City string\n}\n\nfunc main() {\n    // Field names -- order doesn't matter, all fields required unless omitted\n    p1 := Person{Name: \"Alice\", Age: 30, City: \"Boston\"}\n\n    // Positional -- order MUST match the struct definition exactly\n    p2 := Person{\"Bob\", 25, \"Chicago\"}\n\n    // Zero-valued struct, filled in afterward\n    var p3 Person\n    p3.Name = \"Carol\"\n    p3.Age = 28\n\n    fmt.Println(p1, p2, p3)\n    fmt.Println(p1.Name, p1.Age) // Alice 30\n}\n</code></pre>"},{"location":"level-1/06-structs/#structs-are-value-types","title":"Structs are value types","text":"<p>Assigning or passing a struct copies it \u2014 unlike in Java or Python where objects are references. This matters constantly:</p> <pre><code>package main\n\nimport \"fmt\"\n\ntype Point struct {\n    X, Y int\n}\n\nfunc tryToModify(p Point) {\n    p.X = 999 // modifies the COPY, not the caller's original\n}\n\nfunc main() {\n    original := Point{X: 1, Y: 2}\n    tryToModify(original)\n    fmt.Println(original) // {1 2} -- unchanged\n\n    copy := original\n    copy.X = 100\n    fmt.Println(original, copy) // {1 2} {100 2} -- independent\n}\n</code></pre> <p>To actually mutate the caller's struct, pass a pointer (see Module 7).</p>"},{"location":"level-1/06-structs/#nested-structs","title":"Nested structs","text":"<pre><code>package main\n\nimport \"fmt\"\n\ntype Address struct {\n    Street string\n    City   string\n}\n\ntype Employee struct {\n    Name    string\n    Address Address // struct field inside a struct\n}\n\nfunc main() {\n    e := Employee{\n        Name: \"Dana\",\n        Address: Address{\n            Street: \"1 Main St\",\n            City:   \"Denver\",\n        },\n    }\n\n    fmt.Println(e.Address.City) // Denver\n    e.Address.City = \"Boulder\"  // dotted access reaches into nested fields\n    fmt.Println(e)\n}\n</code></pre>"},{"location":"level-1/06-structs/#anonymous-fields-embedding-and-struct-methods","title":"Anonymous fields (embedding) and struct methods","text":"<p>Embedding a type gives the outer struct direct access to the embedded type's fields and methods -- Go's alternative to classical inheritance, covered in depth in Level 2.</p> <pre><code>package main\n\nimport \"fmt\"\n\ntype Animal struct {\n    Name string\n}\n\nfunc (a Animal) Speak() string {\n    return a.Name + \" makes a sound\"\n}\n\ntype Dog struct {\n    Animal // embedded -- Dog \"has\" all of Animal's fields/methods promoted\n    Breed  string\n}\n\nfunc main() {\n    d := Dog{Animal: Animal{Name: \"Rex\"}, Breed: \"Labrador\"}\n    fmt.Println(d.Name)    // Rex -- promoted field, no need for d.Animal.Name\n    fmt.Println(d.Speak()) // Rex makes a sound -- promoted method\n}\n</code></pre>"},{"location":"level-1/06-structs/#struct-tags-and-comparing-structs","title":"Struct tags and comparing structs","text":"<pre><code>package main\n\nimport \"fmt\"\n\ntype Config struct {\n    Host string `json:\"host\"` // tags are metadata, read by reflection-based\n    Port int    `json:\"port\"` // packages like encoding/json (Level 2)\n}\n\nfunc main() {\n    a := Config{Host: \"localhost\", Port: 8080}\n    b := Config{Host: \"localhost\", Port: 8080}\n\n    // Structs with only comparable fields support == directly\n    fmt.Println(a == b) // true -- compares field by field\n}\n</code></pre>"},{"location":"level-1/06-structs/#cheat-sheet","title":"Cheat sheet","text":"Feature Syntax Define <code>type Person struct { Name string; Age int }</code> Create (named fields) <code>Person{Name: \"A\", Age: 1}</code> Create (positional) <code>Person{\"A\", 1}</code> Zero value <code>var p Person</code> Access/set field <code>p.Name</code>, <code>p.Name = \"X\"</code> Nested access <code>e.Address.City</code> Embedding <code>type Dog struct { Animal; Breed string }</code> Compare (comparable fields) <code>a == b</code> Struct tag <code>`json:\"host\"`</code>"},{"location":"level-1/06-structs/#exercise","title":"Exercise","text":"<p>Define a <code>Book</code> struct with <code>Title</code>, <code>Author</code>, and <code>Pages</code> fields. Write a function <code>isLong(b Book) bool</code> that returns whether <code>Pages &gt; 300</code>. In <code>main</code>, create a slice of three <code>Book</code> values and loop over them, printing each title along with whether it's \"long.\"</p>"},{"location":"level-1/07-pointers/","title":"07 \u00b7 Pointers","text":""},{"location":"level-1/07-pointers/#why-go-has-pointers-but-no-pointer-arithmetic","title":"Why Go has pointers (but no pointer arithmetic)","text":"<p>A pointer holds the memory address of a value instead of the value itself. Go uses pointers for two main reasons: letting a function mutate the caller's data, and avoiding expensive copies of large structs. Unlike C, Go pointers are safe \u2014 no arithmetic, and the garbage collector tracks what they point to.</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc main() {\n    x := 42\n    p := &amp;x // &amp; takes the address of x -- p is a *int\n\n    fmt.Println(x)  // 42\n    fmt.Println(p)  // 0xc0000140a0 (some address)\n    fmt.Println(*p) // 42 -- * dereferences: \"the value p points to\"\n\n    *p = 100 // writing through the pointer changes x\n    fmt.Println(x) // 100\n}\n</code></pre> Operator Meaning <code>&amp;x</code> \"Address of x\" \u2014 produces a pointer to <code>x</code> <code>*p</code> \"Value pointed to by p\" \u2014 dereferences the pointer <code>var p *int</code> Declares <code>p</code> as a pointer to an <code>int</code> (zero value: <code>nil</code>)"},{"location":"level-1/07-pointers/#pointers-let-functions-mutate-caller-state","title":"Pointers let functions mutate caller state","text":"<p>Recall from Module 6 that passing a struct by value copies it. Passing a pointer instead lets the function reach back into the caller's original data:</p> <pre><code>package main\n\nimport \"fmt\"\n\ntype Point struct {\n    X, Y int\n}\n\n// pointer receiver -- modifies the CALLER's struct\nfunc movePoint(p *Point, dx, dy int) {\n    p.X += dx // Go auto-dereferences: shorthand for (*p).X\n    p.Y += dy\n}\n\nfunc main() {\n    pt := Point{X: 1, Y: 2}\n    movePoint(&amp;pt, 5, 5)\n    fmt.Println(pt) // {6 7} -- actually changed this time\n}\n</code></pre>"},{"location":"level-1/07-pointers/#new-vs-type","title":"new() vs &amp;Type{}","text":"<pre><code>package main\n\nimport \"fmt\"\n\ntype Counter struct {\n    Count int\n}\n\nfunc main() {\n    // new(T) allocates zeroed memory for T, returns *T\n    c1 := new(Counter)\n    c1.Count = 1\n\n    // &amp;T{} is the more idiomatic way when you also want to set fields\n    c2 := &amp;Counter{Count: 10}\n\n    fmt.Println(c1, c2)   // &amp;{1} &amp;{10}\n    fmt.Println(*c1, *c2) // {1} {10}\n}\n</code></pre> <p><code>&amp;Counter{Count: 10}</code> is by far the more common pattern in real code; <code>new</code> shows up mostly for primitive types or when you want a bare zero value.</p>"},{"location":"level-1/07-pointers/#nil-pointers","title":"nil pointers","text":"<p>A pointer's zero value is <code>nil</code> \u2014 dereferencing a <code>nil</code> pointer panics at runtime, so always check before dereferencing when a pointer might be unset:</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc safePrint(p *int) {\n    if p == nil {\n        fmt.Println(\"no value\")\n        return\n    }\n    fmt.Println(*p)\n}\n\nfunc main() {\n    var p *int    // nil\n    safePrint(p)  // no value\n\n    x := 5\n    safePrint(&amp;x) // 5\n}\n</code></pre>"},{"location":"level-1/07-pointers/#pointers-to-slices-and-maps-usually-unnecessary","title":"Pointers to slices and maps: usually unnecessary","text":"<p>Slices and maps already contain an internal pointer to their underlying data, so passing them by value already lets a function mutate their contents (append is the one exception \u2014 it can return a new slice header):</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc double(nums []int) {\n    for i := range nums {\n        nums[i] *= 2 // mutates the shared backing array directly\n    }\n}\n\nfunc main() {\n    values := []int{1, 2, 3}\n    double(values)\n    fmt.Println(values) // [2 4 6] -- no pointer needed\n}\n</code></pre>"},{"location":"level-1/07-pointers/#cheat-sheet","title":"Cheat sheet","text":"Concept Syntax Address-of <code>p := &amp;x</code> Dereference <code>*p</code> Pointer type <code>var p *int</code> Pointer to struct literal <code>&amp;Point{X: 1, Y: 2}</code> Allocate zeroed <code>p := new(Point)</code> Auto-deref field access <code>p.X</code> (shorthand for <code>(*p).X</code>) Nil check <code>if p == nil { ... }</code>"},{"location":"level-1/07-pointers/#exercise","title":"Exercise","text":"<p>Write a function <code>increment(n *int)</code> that adds 1 to the int a pointer points to. Then write a function <code>resetScores(scores *[]int)</code> that sets the slice a pointer points to back to an empty slice (<code>*scores = []int{}</code>). Call both from <code>main</code> and print the results to confirm the mutations are visible to the caller.</p>"},{"location":"level-1/08-error-handling/","title":"08 \u00b7 Error Handling","text":""},{"location":"level-1/08-error-handling/#errors-are-values-not-exceptions","title":"Errors are values, not exceptions","text":"<p>Go has no <code>try</code>/<code>catch</code>. Instead, any function that can fail returns an <code>error</code> as its last return value \u2014 <code>nil</code> means success, non-nil means failure. The caller is expected to check it immediately.</p> <pre><code>package main\n\nimport (\n    \"errors\"\n    \"fmt\"\n)\n\nfunc divide(a, b float64) (float64, error) {\n    if b == 0 {\n        return 0, errors.New(\"cannot divide by zero\")\n    }\n    return a / b, nil\n}\n\nfunc main() {\n    result, err := divide(10, 2)\n    if err != nil {\n        fmt.Println(\"Error:\", err)\n        return\n    }\n    fmt.Println(\"Result:\", result) // Result: 5\n\n    result, err = divide(10, 0)\n    if err != nil {\n        fmt.Println(\"Error:\", err) // Error: cannot divide by zero\n        return\n    }\n    fmt.Println(\"Result:\", result)\n}\n</code></pre>"},{"location":"level-1/08-error-handling/#the-error-interface","title":"The <code>error</code> interface","text":"<p><code>error</code> is just an interface with one method:</p> <pre><code>type error interface {\n    Error() string\n}\n</code></pre> <p>Anything implementing <code>Error() string</code> can be returned as an <code>error</code> \u2014 this is what lets you build custom, structured error types (explored fully in Level 2). For now, two standard ways to build a plain error message:</p> <pre><code>package main\n\nimport (\n    \"errors\"\n    \"fmt\"\n)\n\nfunc validateAge(age int) error {\n    if age &lt; 0 {\n        return errors.New(\"age cannot be negative\")\n    }\n    if age &gt; 150 {\n        return fmt.Errorf(\"age %d is unrealistically high\", age)\n    }\n    return nil\n}\n\nfunc main() {\n    for _, age := range []int{30, -5, 200} {\n        if err := validateAge(age); err != nil {\n            fmt.Println(\"invalid:\", err)\n            continue\n        }\n        fmt.Println(\"valid age:\", age)\n    }\n}\n</code></pre> <p><code>fmt.Errorf</code> works like <code>fmt.Sprintf</code> but returns an <code>error</code> \u2014 the go-to choice whenever the message needs to include a value.</p>"},{"location":"level-1/08-error-handling/#the-if-err-nil-idiom","title":"The if err != nil idiom","text":"<p>This four-line shape appears after almost every fallible call in Go code:</p> <pre><code>package main\n\nimport (\n    \"fmt\"\n    \"strconv\"\n)\n\nfunc main() {\n    inputs := []string{\"42\", \"abc\", \"17\"}\n\n    for _, s := range inputs {\n        n, err := strconv.Atoi(s)\n        if err != nil {\n            fmt.Printf(\"skipping %q: %v\\n\", s, err)\n            continue\n        }\n        fmt.Println(\"parsed:\", n)\n    }\n    // parsed: 42\n    // skipping \"abc\": strconv.Atoi: parsing \"abc\": invalid syntax\n    // parsed: 17\n}\n</code></pre> <p>It looks repetitive coming from exception-based languages, but the payoff is that every point where something can fail is visible directly in the control flow \u2014 nothing is thrown silently across stack frames.</p>"},{"location":"level-1/08-error-handling/#panic-and-recover-rare-not-your-default-tool","title":"panic and recover (rare, not your default tool)","text":"<p><code>panic</code> stops normal execution immediately; <code>recover</code> (used inside a deferred function) can catch a panic and let the program continue. Reserve these for truly unrecoverable programmer errors (e.g. an invariant that should be impossible) \u2014 never as a substitute for returning an <code>error</code>.</p> <pre><code>package main\n\nimport \"fmt\"\n\nfunc safeDivide(a, b int) (result int, err error) {\n    defer func() {\n        if r := recover(); r != nil {\n            err = fmt.Errorf(\"recovered from panic: %v\", r)\n        }\n    }()\n\n    result = a / b // dividing by zero panics for integers\n    return result, nil\n}\n\nfunc main() {\n    result, err := safeDivide(10, 0)\n    if err != nil {\n        fmt.Println(\"error:\", err) // error: recovered from panic: runtime error: integer divide by zero\n        return\n    }\n    fmt.Println(result)\n}\n</code></pre>"},{"location":"level-1/08-error-handling/#cheat-sheet","title":"Cheat sheet","text":"Task Syntax Return a plain error <code>errors.New(\"message\")</code> Return a formatted error <code>fmt.Errorf(\"bad value: %d\", n)</code> Check for failure <code>if err != nil { ... }</code> Custom error type <code>type X struct{}; func (X) Error() string { ... }</code> Stop execution <code>panic(\"message\")</code> Catch a panic <code>defer func() { recover() }()</code>"},{"location":"level-1/08-error-handling/#exercise","title":"Exercise","text":"<p>Write a function <code>safeSqrt(x float64) (float64, error)</code> that returns an error (<code>\"cannot take sqrt of a negative number\"</code>) if <code>x &lt; 0</code>, and otherwise returns <code>math.Sqrt(x)</code>. Call it in a loop over <code>[]float64{16, -4, 25}</code>, printing either the result or the error using the <code>if err != nil</code> idiom.</p>"},{"location":"level-1/09-packages-modules/","title":"09 \u00b7 Packages &amp; Modules","text":""},{"location":"level-1/09-packages-modules/#packages-gos-unit-of-code-organization","title":"Packages: Go's unit of code organization","text":"<p>Every Go file belongs to a package, declared at the top with <code>package name</code>. Files in the same directory must share the same package name. Code in one package accesses another package's exported names via <code>import</code>.</p> <pre><code>todoapp/\n    go.mod\n    main.go          // package main\n    task/\n        task.go       // package task\n        storage.go    // package task\n</code></pre> <pre><code>// task/task.go\npackage task\n\n// Exported (capitalized) -- visible to other packages\ntype Task struct {\n    Title string\n    Done  bool\n}\n\n// unexported (lowercase) -- visible only inside package task\nfunc normalize(title string) string {\n    return title\n}\n</code></pre> <pre><code>// main.go\npackage main\n\nimport (\n    \"fmt\"\n\n    \"todoapp/task\" // import path derived from the module name + folder\n)\n\nfunc main() {\n    t := task.Task{Title: \"Buy milk\"}\n    fmt.Println(t)\n}\n</code></pre>"},{"location":"level-1/09-packages-modules/#exported-vs-unexported-identifiers","title":"Exported vs unexported identifiers","text":"<p>Go has no <code>public</code>/<code>private</code> keywords. Capitalization is the visibility rule: any function, type, variable, or struct field starting with an uppercase letter is exported (importable elsewhere); lowercase means package-private.</p> <pre><code>package task\n\nvar MaxTasks = 100      // exported -- other packages see task.MaxTasks\nvar defaultPriority = 1 // unexported -- only visible inside this package\n\nfunc NewTask(title string) Task { // exported constructor function\n    return Task{Title: title}\n}\n</code></pre>"},{"location":"level-1/09-packages-modules/#gomod-the-module-file","title":"go.mod: the module file","text":"<p>A module is a collection of packages versioned and distributed together, described by a <code>go.mod</code> file at its root:</p> <pre><code>go mod init todoapp\n# creates go.mod with: module todoapp\n#                       go 1.22\n</code></pre> <pre><code>// go.mod\nmodule todoapp\n\ngo 1.22\n\nrequire (\n    github.com/google/uuid v1.6.0\n)\n</code></pre> <ul> <li><code>module todoapp</code> \u2014 the module's import path root; internal packages are   imported as <code>todoapp/task</code>, <code>todoapp/storage</code>, etc.</li> <li><code>go 1.22</code> \u2014 minimum Go language version this module requires.</li> <li><code>require (...)</code> \u2014 external dependencies and their versions, added   automatically when you <code>go get</code> a package or run <code>go mod tidy</code>.</li> </ul>"},{"location":"level-1/09-packages-modules/#adding-and-tidying-dependencies","title":"Adding and tidying dependencies","text":"<pre><code>go get github.com/google/uuid@v1.6.0   # add a specific version\ngo get github.com/google/uuid          # add the latest version\n\ngo mod tidy   # adds missing requires, removes unused ones,\n              # and updates go.sum (checksums for reproducible builds)\n</code></pre> <p><code>go.sum</code> records cryptographic checksums of every dependency version used, so builds are verifiable and reproducible \u2014 always commit both <code>go.mod</code> and <code>go.sum</code> to version control.</p>"},{"location":"level-1/09-packages-modules/#the-standard-library-is-huge-use-it-first","title":"The standard library is huge \u2014 use it first","text":"<p>Before reaching for a third-party package, check whether the standard library already covers it: <code>fmt</code> (formatting), <code>strings</code>/<code>strconv</code> (text/parsing), <code>os</code> (files, environment, args), <code>net/http</code> (HTTP client and server), <code>encoding/json</code> (JSON), <code>time</code> (dates/durations), <code>sort</code>, <code>errors</code>. Idiomatic Go code leans on the standard library heavily and adds external dependencies sparingly.</p> <pre><code>package main\n\nimport (\n    \"fmt\"\n    \"os\"\n)\n\nfunc main() {\n    fmt.Println(\"args:\", os.Args[1:]) // command-line arguments (excluding program name)\n    home, _ := os.UserHomeDir()\n    fmt.Println(\"home:\", home)\n}\n</code></pre>"},{"location":"level-1/09-packages-modules/#cheat-sheet","title":"Cheat sheet","text":"Task Command / syntax Create a module <code>go mod init &lt;module-name&gt;</code> Add a dependency <code>go get &lt;import-path&gt;@&lt;version&gt;</code> Clean up dependencies <code>go mod tidy</code> Import a package <code>import \"todoapp/task\"</code> Import stdlib package <code>import \"fmt\"</code> Import with alias <code>import f \"fmt\"</code> Exported identifier Starts with an uppercase letter Unexported identifier Starts with a lowercase letter"},{"location":"level-1/09-packages-modules/#exercise","title":"Exercise","text":"<p>Create a new module <code>mathutils</code> with <code>go mod init mathutils</code>. Add a subpackage <code>calc</code> (folder <code>calc/</code>) with an exported function <code>Square(n int) int</code> and an unexported helper it calls internally. Import <code>mathutils/calc</code> from a <code>main.go</code> at the module root and print <code>calc.Square(7)</code>.</p>"},{"location":"level-1/10-project-todo-cli/","title":"10 \u00b7 Project \u2014 CLI To-Do App","text":"<p>A small end-to-end project combining everything from Level 1: structs, slices, pointers, error handling, and packages/modules, with persistence to a JSON file on disk.</p>"},{"location":"level-1/10-project-todo-cli/#what-youll-build","title":"What you'll build","text":"<p>A command-line to-do app that:</p> <ul> <li>Adds tasks</li> <li>Lists all tasks (done and pending)</li> <li>Marks a task done by its number</li> <li>Deletes a task by its number</li> <li>Persists everything to <code>tasks.json</code> between runs</li> </ul>"},{"location":"level-1/10-project-todo-cli/#project-layout","title":"Project layout","text":"<pre><code>todocli/\n    go.mod\n    main.go\n    todo/\n        task.go\n        store.go\n</code></pre>"},{"location":"level-1/10-project-todo-cli/#setting-up-the-module","title":"Setting up the module","text":"<pre><code>mkdir todocli &amp;&amp; cd todocli\ngo mod init todocli\nmkdir todo\n</code></pre>"},{"location":"level-1/10-project-todo-cli/#todotaskgo-the-data-type","title":"todo/task.go \u2014 the data type","text":"<pre><code>// todo/task.go\npackage todo\n\n// Task is a single to-do item.\ntype Task struct {\n    ID    int    `json:\"id\"`\n    Title string `json:\"title\"`\n    Done  bool   `json:\"done\"`\n}\n</code></pre>"},{"location":"level-1/10-project-todo-cli/#todostorego-json-persistence","title":"todo/store.go \u2014 JSON persistence","text":"<pre><code>// todo/store.go\npackage todo\n\nimport (\n    \"encoding/json\"\n    \"fmt\"\n    \"os\"\n)\n\n// Store manages loading and saving Tasks to a JSON file.\ntype Store struct {\n    path string\n}\n\n// NewStore returns a Store backed by the given file path.\nfunc NewStore(path string) *Store {\n    return &amp;Store{path: path}\n}\n\n// Load reads all tasks from disk. A missing file is not an error --\n// it just means there are no tasks yet.\nfunc (s *Store) Load() ([]Task, error) {\n    data, err := os.ReadFile(s.path)\n    if os.IsNotExist(err) {\n        return []Task{}, nil\n    }\n    if err != nil {\n        return nil, fmt.Errorf(\"reading tasks file: %w\", err)\n    }\n\n    var tasks []Task\n    if err := json.Unmarshal(data, &amp;tasks); err != nil {\n        return nil, fmt.Errorf(\"parsing tasks file: %w\", err)\n    }\n    return tasks, nil\n}\n\n// Save writes all tasks back to disk as indented JSON.\nfunc (s *Store) Save(tasks []Task) error {\n    data, err := json.MarshalIndent(tasks, \"\", \"  \")\n    if err != nil {\n        return fmt.Errorf(\"encoding tasks: %w\", err)\n    }\n    if err := os.WriteFile(s.path, data, 0644); err != nil {\n        return fmt.Errorf(\"writing tasks file: %w\", err)\n    }\n    return nil\n}\n\n// NextID returns the smallest unused, positive ID for a new task.\nfunc NextID(tasks []Task) int {\n    max := 0\n    for _, t := range tasks {\n        if t.ID &gt; max {\n            max = t.ID\n        }\n    }\n    return max + 1\n}\n</code></pre> <p><code>%w</code> in <code>fmt.Errorf</code> wraps the underlying error so callers can inspect the original cause with <code>errors.Is</code>/<code>errors.Unwrap</code> \u2014 you'll use this properly in Level 2, Module 4.</p>"},{"location":"level-1/10-project-todo-cli/#maingo-the-cli","title":"main.go \u2014 the CLI","text":"<pre><code>// main.go\npackage main\n\nimport (\n    \"fmt\"\n    \"os\"\n    \"strconv\"\n\n    \"todocli/todo\"\n)\n\nconst dataFile = \"tasks.json\"\n\nfunc main() {\n    store := todo.NewStore(dataFile)\n    tasks, err := store.Load()\n    if err != nil {\n        fmt.Println(\"error loading tasks:\", err)\n        os.Exit(1)\n    }\n\n    if len(os.Args) &lt; 2 {\n        printUsage()\n        return\n    }\n\n    command := os.Args[1]\n    args := os.Args[2:]\n\n    switch command {\n    case \"add\":\n        tasks, err = addTask(tasks, args)\n    case \"list\":\n        listTasks(tasks)\n        return\n    case \"done\":\n        tasks, err = markDone(tasks, args)\n    case \"delete\":\n        tasks, err = deleteTask(tasks, args)\n    default:\n        printUsage()\n        return\n    }\n\n    if err != nil {\n        fmt.Println(\"error:\", err)\n        os.Exit(1)\n    }\n\n    if err := store.Save(tasks); err != nil {\n        fmt.Println(\"error saving tasks:\", err)\n        os.Exit(1)\n    }\n}\n\nfunc printUsage() {\n    fmt.Println(\"Usage: todocli [add &lt;title&gt; | list | done &lt;id&gt; | delete &lt;id&gt;]\")\n}\n\nfunc addTask(tasks []todo.Task, args []string) ([]todo.Task, error) {\n    if len(args) != 1 {\n        return tasks, fmt.Errorf(\"add requires exactly one quoted title\")\n    }\n    newTask := todo.Task{\n        ID:    todo.NextID(tasks),\n        Title: args[0],\n        Done:  false,\n    }\n    tasks = append(tasks, newTask)\n    fmt.Printf(\"Added #%d: %s\\n\", newTask.ID, newTask.Title)\n    return tasks, nil\n}\n\nfunc listTasks(tasks []todo.Task) {\n    if len(tasks) == 0 {\n        fmt.Println(\"No tasks yet.\")\n        return\n    }\n    for _, t := range tasks {\n        status := \" \"\n        if t.Done {\n            status = \"x\"\n        }\n        fmt.Printf(\"[%s] #%d %s\\n\", status, t.ID, t.Title)\n    }\n}\n\nfunc markDone(tasks []todo.Task, args []string) ([]todo.Task, error) {\n    id, err := parseID(args)\n    if err != nil {\n        return tasks, err\n    }\n    for i := range tasks {\n        if tasks[i].ID == id {\n            tasks[i].Done = true\n            fmt.Printf(\"Marked #%d done\\n\", id)\n            return tasks, nil\n        }\n    }\n    return tasks, fmt.Errorf(\"no task with id %d\", id)\n}\n\nfunc deleteTask(tasks []todo.Task, args []string) ([]todo.Task, error) {\n    id, err := parseID(args)\n    if err != nil {\n        return tasks, err\n    }\n    remaining := make([]todo.Task, 0, len(tasks))\n    found := false\n    for _, t := range tasks {\n        if t.ID == id {\n            found = true\n            continue\n        }\n        remaining = append(remaining, t)\n    }\n    if !found {\n        return tasks, fmt.Errorf(\"no task with id %d\", id)\n    }\n    fmt.Printf(\"Deleted #%d\\n\", id)\n    return remaining, nil\n}\n\nfunc parseID(args []string) (int, error) {\n    if len(args) != 1 {\n        return 0, fmt.Errorf(\"expected exactly one id argument\")\n    }\n    id, err := strconv.Atoi(args[0])\n    if err != nil {\n        return 0, fmt.Errorf(\"invalid id %q: %w\", args[0], err)\n    }\n    return id, nil\n}\n</code></pre>"},{"location":"level-1/10-project-todo-cli/#running-it","title":"Running it","text":"<pre><code>go run . add \"Buy milk\"\n# Added #1: Buy milk\n\ngo run . add \"Write report\"\n# Added #2: Write report\n\ngo run . list\n# [ ] #1 Buy milk\n# [ ] #2 Write report\n\ngo run . done 1\n# Marked #1 done\n\ngo run . list\n# [x] #1 Buy milk\n# [ ] #2 Write report\n\ngo run . delete 2\n# Deleted #2\n\ngo run . list\n# [x] #1 Buy milk\n</code></pre> <p>Each invocation reloads <code>tasks.json</code> from disk, so tasks persist across separate runs of the program. Build a standalone binary with <code>go build -o todocli .</code> and <code>./todocli list</code> works the same way, no <code>go run</code> needed.</p>"},{"location":"level-1/10-project-todo-cli/#stretch-goals","title":"Stretch goals","text":"<ul> <li>Add a <code>priority</code> field (<code>low</code>/<code>medium</code>/<code>high</code>) and sort <code>list</code> output by   it.</li> <li>Add a <code>--filter=done</code> / <code>--filter=pending</code> flag to <code>list</code> (you'll build   proper flag parsing with the <code>flag</code> package in   Level 3, Module 8).</li> <li>Write unit tests for <code>NextID</code> and the add/done/delete logic (formalized   with the <code>testing</code> package in   Level 2, Module 6).</li> <li>Switch from a full-file rewrite on every save to an append-only log file.</li> </ul> <p>Completing this project means you're ready for Level 2 \u00b7 Intermediate.</p>"},{"location":"level-2/","title":"Level 2 \u00b7 Intermediate Idiomatic Go","text":""},{"location":"level-2/#modules","title":"Modules","text":"<ol> <li>Interfaces</li> <li>Goroutines &amp; Channels Basics</li> <li>Methods &amp; Receivers</li> <li>Custom Errors &amp; Error Wrapping</li> <li>JSON Encoding/Decoding</li> <li>Testing with the testing Package</li> <li>File I/O</li> <li>Working with net/http</li> <li>Dependency Management</li> <li>Project \u2014 Weather CLI</li> </ol>"},{"location":"level-2/01-interfaces/","title":"01 \u00b7 Interfaces","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Defining and satisfying interfaces implicitly, the empty interface.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-2/02-goroutines-channels/","title":"02 \u00b7 Goroutines &amp; Channels Basics","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Launching goroutines, unbuffered/buffered channels, basic synchronization.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-2/03-methods-receivers/","title":"03 \u00b7 Methods &amp; Receivers","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Value vs pointer receivers, method sets.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-2/04-custom-errors-wrapping/","title":"04 \u00b7 Custom Errors &amp; Error Wrapping","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Custom error types, errors.Is/As, %w wrapping.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-2/05-json-encoding-decoding/","title":"05 \u00b7 JSON Encoding/Decoding","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. encoding/json, struct tags, marshal/unmarshal.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-2/06-testing-package/","title":"06 \u00b7 Testing with the testing Package","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Writing tests with go test, table-driven tests basics.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-2/07-file-io/","title":"07 \u00b7 File I/O","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Reading and writing files with os and bufio.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-2/08-net-http-client/","title":"08 \u00b7 Working with net/http","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Making HTTP requests with the standard library client.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-2/09-dependency-management/","title":"09 \u00b7 Dependency Management","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. go.mod, go.sum, versioning, and the Go module proxy.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-2/10-project-weather-cli/","title":"10 \u00b7 Project \u2014 Weather CLI","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. A CLI that consumes a public weather API and reports results.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-3/","title":"Level 3 \u00b7 Advanced Production-ready","text":""},{"location":"level-3/#modules","title":"Modules","text":"<ol> <li>Concurrency Patterns</li> <li>Building REST APIs</li> <li>Databases with database/sql</li> <li>The Context Package</li> <li>Design Patterns in Go</li> <li>Testing Advanced</li> <li>Profiling &amp; Benchmarking</li> <li>Building CLIs</li> <li>Interfaces &amp; Generics</li> <li>Project \u2014 REST API + SQLite Service</li> </ol>"},{"location":"level-3/01-concurrency-patterns/","title":"01 \u00b7 Concurrency Patterns","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. select, the sync package, and worker pool patterns.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-3/02-building-rest-apis/","title":"02 \u00b7 Building REST APIs","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. net/http servers, routing, and handler patterns.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-3/03-databases-sql/","title":"03 \u00b7 Databases with database/sql","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Connecting to SQLite, queries, and scanning rows.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-3/04-context-package/","title":"04 \u00b7 The Context Package","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Cancellation, deadlines, and passing request-scoped values.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-3/05-design-patterns/","title":"05 \u00b7 Design Patterns in Go","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Idiomatic Go takes on common design patterns.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-3/06-testing-advanced/","title":"06 \u00b7 Testing Advanced","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Table-driven tests, mocks, and test coverage.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-3/07-profiling-benchmarking/","title":"07 \u00b7 Profiling &amp; Benchmarking","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. pprof and the testing package's benchmark support.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-3/08-building-clis/","title":"08 \u00b7 Building CLIs","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. The flag package and building subcommands.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-3/09-interfaces-generics/","title":"09 \u00b7 Interfaces &amp; Generics","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Type parameters and generic functions/types (Go 1.18+).</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-3/10-project-rest-api-sqlite/","title":"10 \u00b7 Project \u2014 REST API + SQLite Service","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. A REST API service backed by a SQLite database.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-4/","title":"Level 4 \u00b7 Master Production systems","text":""},{"location":"level-4/#modules","title":"Modules","text":"<ol> <li>Advanced Concurrency</li> <li>Microservices Patterns in Go</li> <li>gRPC Basics</li> <li>Production-Grade APIs</li> <li>Testing at Scale &amp; CI</li> <li>Deployment (Docker for Go)</li> <li>Observability</li> <li>Security Best Practices</li> <li>Performance Optimization</li> <li>Capstone Project</li> </ol>"},{"location":"level-4/01-advanced-concurrency/","title":"01 \u00b7 Advanced Concurrency","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Context cancellation and error groups for coordinated goroutines.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-4/02-microservices-patterns/","title":"02 \u00b7 Microservices Patterns in Go","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Service boundaries and communication patterns in Go.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-4/03-grpc-basics/","title":"03 \u00b7 gRPC Basics","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Protocol buffers and a minimal gRPC service.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-4/04-production-grade-apis/","title":"04 \u00b7 Production-Grade APIs","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Middleware, authentication, and API design at scale.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-4/05-testing-at-scale-ci/","title":"05 \u00b7 Testing at Scale &amp; CI","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Organizing large Go test suites and CI integration.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-4/06-deployment-docker/","title":"06 \u00b7 Deployment (Docker for Go)","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Multi-stage Docker builds for small Go binaries.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-4/07-observability/","title":"07 \u00b7 Observability","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Structured logging and metrics for Go services.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-4/08-security-best-practices/","title":"08 \u00b7 Security Best Practices","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Common Go security pitfalls and how to avoid them.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-4/09-performance-optimization/","title":"09 \u00b7 Performance Optimization","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. Memory allocation patterns and optimization techniques.</p> <p>More lessons are on the way \u2014 check back soon.</p>"},{"location":"level-4/10-capstone-project/","title":"10 \u00b7 Capstone Project","text":"<p>Coming soon</p> <p>This module's full lessons and code snippets are being written next. A production-shaped microservice with tests, Docker, and CI.</p> <p>More lessons are on the way \u2014 check back soon.</p>"}]}